Algoritmo "total a pagar"

Var
      produto,porcentagem,total: real


Inicio
          escreval("valor do produto: ")
          leia(produto)
          escreval("valor de porcentagem do desconto: ")
          leia(porcentagem)
          total <- (produto - porcentagem) /  1
          escreval("total a pagar: ",total)


Fimalgoritmo